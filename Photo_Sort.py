#!/usr/bin/env python3

import os
import shutil

DIRECTORY = input("What is the name of the Directory? ")

# ask for the name of the directory

# create a directory for jpg

# create a directory for raw files

# rename the files based on the requested directory name

# rename the files based on creation date?

# rename the files with a custom date?